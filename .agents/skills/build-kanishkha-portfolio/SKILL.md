---
name: build-kanishkha-portfolio
description: Build, verify, and maintain Kanishkha Korrapati Anna's React finance portfolio. Use for content edits, responsive UI work, resume updates, accessibility checks, GitHub Pages preparation, or executing tasks from the project tracker.
---

# Build Kanishkha Portfolio

1. Read `../../../MEMORY.md`, then `../../../../docs/PROJECT_PLAN.md`.
2. Read `references/content-routing.md` and load only the files required for the selected task.
3. Change the smallest coherent surface. Preserve verified content and the current visual system unless the task explicitly changes them.
4. Update the task tracker alongside the implementation.
5. Run the relevant checks. For any shipped change, run `npm run build` and search `src`, `public/index.html`, and `package.json` for stale Tanmayi content.
6. Report implemented scope, verification evidence, and remaining release blockers.

Never infer missing profile URLs or achievements. Add a claim only when it exists in the normalized source or a newly supplied artifact.
