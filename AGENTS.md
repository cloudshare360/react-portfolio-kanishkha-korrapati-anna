# Agent operating guide

Use `.agents/skills/build-kanishkha-portfolio/SKILL.md` for portfolio changes. Read `.agents/MEMORY.md` first. Treat `docs/CONTENT_SOURCE_OF_TRUTH.md` as the normalized factual source; open the binary resumes only when auditing extraction or resolving missing information. Track work in `docs/PROJECT_PLAN.md` and update task status in the same change.

Do not fabricate URLs, metrics, dates, projects, or credentials. Run a production build and search shipped files for stale Tanmayi content before handing off.
